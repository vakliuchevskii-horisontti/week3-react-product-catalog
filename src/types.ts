export interface User {
  id: number;
  name: string;
  email: string;
  phone: string;
}

export interface StoreItem {
  id: number;
  title: string;
  price: number;
  image: string;
  description: string;
}
